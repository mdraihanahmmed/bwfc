import React from "react";

const Headingsmall = ({ text }) => {
  return (
    <div>
      <h4 className="font-man text-lg font-extrabold uppercase text-primary">
        {text}
      </h4>
    </div>
  );
};

export default Headingsmall;
